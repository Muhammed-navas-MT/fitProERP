export enum SlotAndBookingError {
  ALREADY_BOOKED = "This slot is already booked",
}

export enum SlotAndBookingSuccess {
  LISTED = "List all available slot successfully",
  LISTED_SESSION = "List all sessions successfully",
}
